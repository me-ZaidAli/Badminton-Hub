Improve the existing merchandise admin section in my badminton app without changing or redesigning the current user-facing merchandise UI and without removing any working features; enhance it by enforcing strict role-based access where GOD MODE (super admin) can manage all products and orders across all clubs and assign products to multiple clubs, while admins can only see and manage merchandise and orders within their own clubs; add a real-time notification badge in the menu showing the number of new/unprocessed orders (increasing when orders are placed and clearing when viewed) and visually highlight new orders; refactor the admin panel into clear Products and Orders tabs while keeping existing structure, improving readability and usability; in the Products section add multi-club assignment (GOD MODE can select all clubs, admins only their own), product variations (size, colour), image upload, active/inactive toggle, total orders per product, and a stock management system including total stock and optional stock per variation, with clear display and low-stock warnings; implement stock logic so stock does not change when an order is created, decreases only when an order is marked as Processing or Completed, and is restored if an order is cancelled, with validation to prevent overselling and blocking orders when stock is insufficient or zero; in the Orders section upgrade the table to include player name, club, product, variation, quantity, total price, payment status, order status, and date, with search, filters (club for GOD MODE, payment status, order status, date), sorting and pagination; add a standard order status system (New, Processing, Ready for Collection, Completed, Cancelled) and payment status (Paid/Unpaid), with fully functional actions including mark as paid/unpaid, change status, edit order, delete with confirmation, and automatic UI updates; add an order detail modal or side panel showing full player details, order breakdown, payment status, notes, and status timeline; include bulk actions (select multiple orders to mark as paid or update status); ensure all data is correctly linked between users, clubs, products and orders, enforce permissions at both UI and API level, ensure all buttons are fully functional with no placeholders, maintain backward compatibility with existing data, and ensure all updates (orders, stock, payments) are consistent, accurate and reflected in real time; the admin panel UI must be modern, premium and “next-level” with a clean dashboard feel (similar to high-end SaaS products), including card-based summary at the top (total orders, pending, unpaid, revenue, low stock), smooth transitions, side panels instead of page reloads, clear visual hierarchy, responsive layout, subtle animations, highlighted states for new/unpaid/low stock items, intuitive spacing and typography, and an overall fast, minimal, professional experience that allows efficient management across multiple clubs without clutter.
