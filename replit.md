# Club Master

Club Master is a comprehensive web application for racket sports clubs, streamlining operations and enhancing player engagement.

## Run & Operate

_Populate as you build_

## Stack

- **Frontend**: React 18, TypeScript, Wouter, TanStack React Query, Tailwind CSS, shadcn/ui
- **Backend**: Node.js, Express.js, TypeScript
- **Database**: PostgreSQL with Drizzle ORM
- **Build Tools**: Vite (frontend), esbuild (backend)
- **Other**: express-session, passport/passport-local, date-fns, recharts, framer-motion

## Where things live

- `client/`: Frontend source code.
  - `client/src/index.css`: Base CSS tokens for "Elite Sports" theme.
  - `client/src/hooks/use-theme.ts`: Theme application classes.
- `server/`: Backend source code.
- `drizzle/schema.ts`: Database schema definition.

## Architecture decisions

- **Theming**: 64-theme Premium Theme System with global Ultra-Premium Glass UI. Default "Dribbble Blue-Violet" in `client/src/index.css` `:root`/`.dark` (deep blue-violet bg, violet accent `252 90% 68%`). Body radial wash on `html.theme-default`. Some legacy components (`Dashboard.tsx`, `Deals.tsx`, `MerchandisePage.tsx`, `not-found.tsx`) bypass tokens — pending refactor.
- **Matchmaking**: Smart Match Engine v6 (gender-aware, 9-tier grade scoring) + BPG Competitive Balance + Session Fairness Command Center.
- **Freemium**: 2-plan (Basic FREE / Premium), backend-enforced + frontend-gated.
- **Multi-Tenancy**: Multi-club + multi-sport with role-based access.
- **Club Control Center** (`/admin/control-center`, `client/src/pages/admin/ClubControlCenter.tsx`): Consolidated hub. OWNER sees all clubs (`/api/super-admin/clubs/billing`); ADMIN sees own. Per-club feature toggles in `clubs.featureOverrides` jsonb (`GET/PATCH /api/clubs/:id/feature-overrides`, PATCH = OWNER-only).
- **Localization**: All event date/time anchored to `Europe/London`.
- **Player Rankings UI** (`client/src/pages/PlayerRankings.tsx`): Top-3 = `HeroPlayerCard` (gender-aware hero photo, violet glow), ranks 4+ = `CompactPlayerCard`. All surfaces use `hsl(var(--card)/x)` tokens.

### BSL Share Invite (link + QR)
Reusable `client/src/pages/bsl/components/ShareInviteDialog.tsx` shows a copy-able shareable URL, an `<img>`-rendered QR code (via `https://api.qrserver.com/v1/create-qr-code/`), the invite code chip (when applicable), and three actions: **Copy QR** (uses `navigator.clipboard.write([new ClipboardItem({"image/png": blob})])` so it lands in chats/docs as a real image), **Save PNG** (downloads via blob), and **Share** (Web Share API with link-copy fallback). Wired in: `LeagueMode.tsx` → `?Share BSL` button (URL `/bsl`, no code), `ClubManager.tsx` → `Share invite` button on the manager header (URL `/bsl/join?code=<inviteCode>`), `ClubsAdmin.tsx` → per-row `Share` action button. `JoinPlayer.tsx` reads `?code=` via `useSearch` and auto-validates the code on mount via the existing `/api/bsl/clubs` lookup. Externals landing on `/bsl/join?code=…` while logged out are bounced to `/login?next=…`: `PrivateRoute` in `App.tsx` now appends the current `location + window.location.search` as `next=`. `Login.tsx` and `Register.tsx` both consume the `next` query param via `safeNext()` (only allows internal `/`-prefixed paths, rejects `//`-prefixed open redirects) and redirect there on success. Register page also gained an optional **Request to join clubs** multi-select chip group (`joinClubIds`) — after account creation it fetches `/api/clubs/:clubId/membership-plans`, picks the cheapest annual plan, and POSTs `/api/membership-requests` per club (failures are non-fatal). The `/api/clubs` query no longer requires `isTrialPlayer` to load, so the chip group is always populated.

### Birmingham Super League (BSL)
Esports-style competition module at `/bsl`. Locked palette: bg `222 50% 6%`, cyan `195 100% 60%`, gold `42 95% 55%` — no other hues.
- **Public routes**: `/bsl` (LeagueMode hub), `/bsl/register-club` (7-step wizard), `/bsl/join` (invite-code), `/bsl/wallet`, `/bsl/match/:id`, `/bsl/profile` (player), `/bsl/my-club` (manager).
- **Admin Control Panel** (`/bsl/admin/*`, OWNER/ADMIN, sidebar "BSL · Control Panel"): Dashboard, league (divisions/fixtures/days), match-day (drag-drop courts), clubs (Mark paid/unpaid via `PATCH /api/bsl/admin/clubs/:id/payment-status` — `PENDING_PAYMENT` hides club from public list), players (assign/stats/wallet/discipline), payments (Pending/History + CSV), media, settings. Audit via `audit()` helper → `bsl_audit_log`.
- **Backend**: `server/bsl-routes.ts`. Schema in `shared/schema.ts` (5 enums + 10 tables: bslLeagues/Clubs/Teams/Players/LeagueDays/Fixtures/Rubbers/WalletTransactions/AuditLog/Media).
- **Club-vs-Club Fixtures + Drag-Drop Pair Assignment**: `bsl_fixtures` has nullable `home_club_id`/`away_club_id` (legacy `home_team_id`/`away_team_id` also nullable). `bsl_rubbers` has nullable `home_team_id`/`away_team_id` per rubber. `POST /api/bsl/admin/club-fixtures` creates fixture + 6 rubbers (default `[MD,MD,WD,WD,XD,XD]`). `PATCH /api/bsl/admin/rubbers/:id/assign` validates club + category match; mirrors pair players into `home/awayPlayer1/2Id` for scoring. Setup UI: `client/src/pages/bsl/admin/FixtureSetup.tsx` (3-column board with HTML5 DnD + select fallback). Top-of-page actions add **+ MD/WD/XD/MS1/MS2/WS** chips (`POST /api/bsl/admin/fixtures/:id/add-rubber`), per-rubber Trash button (`DELETE /api/bsl/admin/rubbers/:id?force=true|false` — refuses non-empty unless `force`, renumbers remaining rubbers 1..N inside `db.transaction`), and two auto-fill buttons: **Auto-generate (vs all)** and **Pair-by-pair** (`POST /api/bsl/admin/fixtures/:id/auto-generate-rubbers {mode: vs_all|parallel, replace, categories?}`). vs_all = Cartesian product of home × away pairs per category (one rubber per matchup); parallel = pair index-by-index. Mirrors players into homePlayer1/2 + awayPlayer1/2. All four endpoints lifecycle-guarded via `assertFixtureMutable`. Audit: `CREATE_CLUB_FIXTURE`, `ASSIGN_RUBBER`, `ADD_RUBBER`, `DELETE_RUBBER`, `AUTO_GENERATE_RUBBERS`.

- **Category Competition Settings + Snapshot-on-Generate** (`/bsl/admin/competition`, `client/src/pages/bsl/admin/CompetitionSettings.tsx`): Replaces hardcoded MS1/MS2/WS/MD/WD/XD lineup + 6 rubbers + 3 sets × 21 pts. Tables: `bsl_category_settings` (UNIQUE leagueId+category — rubbersPerFixture, rubberLineup text[], setsPerMatch, pointsPerSet, scoringRule DEUCE|GOLDEN_POINT|RALLY, deuceCap, format ROUND_ROBIN|KNOCKOUT|GROUPS, courtPool int[], walkoverPolicy, walkoverScore, tiebreakOrder text[] of POINTS|RUBBER_DIFF|RUBBERS_FOR|HEAD_TO_HEAD|MATCHES_WON, pointsWin/Draw/Loss, notes), `bsl_fixture_versions` (archive payload jsonb per Regenerate). New columns: `bsl_league_days.state` (DRAFT|PUBLISHED|LIVE|CLOSED) + `division`/`category`; `bsl_fixtures.category`/`version`/`rulesSnapshot`/`walkoverWinner`/`liveStartedAt`/`livePausedAt`; `bsl_rubbers.setScores` jsonb + `walkoverWinner`. Endpoints in `server/bsl-routes.ts`: `GET /api/bsl/admin/category-settings`, `PUT /api/bsl/admin/category-settings/:category` (sanitised upsert via `sanitiseSettings()` clamping ints + filtering enum tokens), `DELETE /api/bsl/admin/category-settings/:category`, `POST /api/bsl/admin/fixtures/regenerate {division, category?, leagueDayId?, reason?}` (archives only SCHEDULED/WARMUP fixtures into `bsl_fixture_versions` then rebuilds at `version+1` using current settings — LIVE/FINISHED preserved), `GET /api/bsl/admin/fixture-versions[?leagueDayId&division&category]` + `/:id`, `PATCH /api/bsl/admin/league-days/:id/state {state}` (LIVE blocked when 0 fixtures; CLOSED triggers `recomputeStandings`), `POST /api/bsl/admin/fixtures/:id/live {action: start|pause|resume|finish|warmup|reset}`, `POST /api/bsl/admin/rubbers/:id/walkover {winner: HOME|AWAY|NONE}` (writes walkoverScore from snapshot + recomputes fixture rubber tally). Both `POST /api/bsl/admin/fixtures/generate` and `POST /api/bsl/admin/club-fixtures` now accept optional `category` and pull `rubbersPerFixture` + `rubberLineup` from settings, snapshotting the whole settings row onto `bsl_fixtures.rulesSnapshot` so mid-season rule changes don't retroactively rewrite finished games. `recomputeStandings` reads `pointsWin/Draw/Loss` from fixture snapshot first, falls back to category-settings, then league defaults. Frontend uses `BSL` palette only. Sidebar entry "Competition Rules" added in `AdminLayout.tsx` between League Control and Match Day.
- **Super-Admin Club/Player Controls** (`/bsl/admin/clubs`, `/bsl/admin/players`, `/bsl/admin/clubs/:id/manage`): OWNER/ADMIN can spin up clubs/players without going through the public wizard. Endpoints in `server/bsl-routes.ts` (~lines 806-1060): `GET /api/bsl/admin/users/search?q=`, `POST /api/bsl/admin/clubs` (auto-creates pair rows per `categoryPairs`, optional `managerUserId`, status `ACTIVE|PENDING_PAYMENT`, returns invite code when ACTIVE), `POST /api/bsl/admin/players` (find user + attach to club, optional `activate:true`), `POST /api/bsl/admin/players/:id/activate` (force-active, skip payment), `POST /api/bsl/admin/players/:id/wallet/adjust` `{amount,note}` (atomic `db.transaction`, writes properly-typed `TOPUP`/`DEDUCTION` row to `bsl_wallet_transactions` + mirrors into `bslPlayers.walletBalance`), `POST/DELETE /api/bsl/admin/players/:id/categories[/:cat]` (admin override — no fee charged, no balance check), `GET /api/bsl/admin/clubs/:id/manager-view` (returns the same shape as `/bsl/my-club` for any club). All audit-logged. Frontend: `ClubsAdmin.tsx` "+ New club" button + `CreateClubDialog`, "Manage" link per row → `AdminClubManager.tsx` (full roster + per-category pair board, reuses existing manager endpoints which already accept admin via `loadClubForManager()` + `isAdminish()`). `PlayersAdmin.tsx` "+ Create player" button + `CreatePlayerDialog` (user search + club picker + auto-activate toggle), enhanced `PlayerEditor` with Force-Activate panel, Wallet Adjust (Credit/Deduct + amount/note + live balance), Categories chip toggles (MD/WD/XD), and a "Manage roster + pairs" link to `AdminClubManager`. **Wallet enum gotcha**: `bslWalletTxTypeEnum` is `TOPUP | DEDUCTION` (NEVER `DEBIT`).

- **Club Manager Dashboard** (`/bsl/my-club`, `client/src/pages/bsl/ClubManager.tsx`): 6-tile strip (Roster/Pairs/Wins/Win%/Matches/Money in) + Members table (P/W/L/£paid/Edit/Remove). Manager actions via `loadClubForManager()` helper: edit club, withdraw (`bslClubs.withdrawnAt` + admin-only `POST /api/bsl/clubs/:id/reinstate`), confirm join requests, remove players, manage pairs (`bsl_team_members` join table, max 2 per pair, auto-removes from sibling pairs in same category). `PATCH /api/bsl/clubs/:clubId/players/:playerId` edits displayName/bio (audit `MANAGER_UPDATE_PLAYER`). Pair dropdowns dedup placed players via `placedByCat` Set. Top Performers on `/bsl` filters out `matchesPlayed=0` and renders hydrated `displayName`.
- **Player Profile Dashboard** (`/bsl/profile`, `client/src/pages/bsl/PlayerProfile.tsx`): Single round-trip `GET /api/bsl/players/me/dashboard`. Per-fixture home/away perspective derived from actual rubber slot (prevents WIN/LOSS inversion when membership stale). Hero with W/L/Win% strip, per-category pair cards, next-match countdown, last-20 history. Edit displayName/bio via `PATCH /api/bsl/players/me`. Register/unregister categories via `POST/DELETE /api/bsl/players/me/categories` — atomic conditional UPDATE with SQL `CASE` for tier pricing: 1st cat full, 2nd 50% off, 3rd 70% off, keyed off `array_length(categories,1)` so concurrent registrations can't both claim cheaper tier. Charged amount derived from balance delta, recorded as DEBIT `bsl_wallet_transactions`. Per-cat fees in `bslLeagues.categoryFees` jsonb (defaults `{MD:2500,WD:2500,XD:3000}` pence). Toast strips `"402: "` prefix from apiRequest errors.
- **NOTE**: `npm run db:push` may hang on interactive prompts for new tables — use `psql "$DATABASE_URL"` with `ALTER TABLE … ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS` instead.

- **BSL Year-End Prize Vault** (public `/bsl/prizes`, admin `/bsl/admin/prizes`, sidebar "Prize Vault"): Fortnite-style season-pass page where every division × category gets its own glowing tier card (DIAMOND/PLATINUM/GOLD/SILVER/BRONZE/MYTHIC/EPIC — only the trophy badges break the locked BSL palette; chrome stays cyan/gold/bg). Hero shows total prize pool + division/tier counts. Cards use conic-gradient ring + shield clip-path + sparkle dots for rank #1. Table `bsl_prizes` (bslLeagueId FK→bslLeagues, nullable division/category, rank, tier, title, subtitle, prizeText, prizeAmountPence, isPublished, sortOrder). Endpoints in `server/bsl-routes.ts`: `GET /api/bsl/prizes` (public, filters `isPublished` for non-admins via `isAdminish`), `POST/PATCH/DELETE /api/bsl/admin/prizes` (requireAdmin, all field-validated + clamped + audit-logged), `POST /api/bsl/admin/prizes/seed {replace?}` auto-creates a div × MD/WD/XD × top-3 grid using the actual league row's id (returns 400 if no league configured). Admin page has live tier preview + show/hide toggle + Seed defaults + Wipe-and-seed. Quick-action button on `/bsl` LeagueMode hero.

- **BSL Match Days hub** (`/bsl/admin/match-days`, `client/src/pages/bsl/admin/MatchDaysAdmin.tsx`, sidebar entry "Match Days" — old drag-drop courts page renamed "Match Day Live"): Card grid of every league day with state pill + rubbers badge + venue. Click any card to open `MatchDayEditor` modal — full-detail editor for date/time, venue, division, category, rubbers/fixture, legacy status, notes, lifecycle state buttons (DRAFT→PUBLISHED→LIVE→CLOSED), plus inline "Teams playing" table where each fixture row has home club / away club selects, court number, start time, link to pair setup. Add-fixture button creates a new club-vs-club fixture on this day (respects category default). Delete-day button (DRAFT/PUBLISHED only). Single round trip via new `GET /api/bsl/admin/league-days/:id/details` (returns day + hydrated fixtures with club names/logos + rubber counts). New columns `bsl_league_days.venue` text + `notes` text accepted by both `POST` + `PATCH /api/bsl/admin/league-days` (240 / 2000 char caps). `PATCH /api/bsl/fixtures/:id` extended to accept `homeClubId` / `awayClubId` / `bslLeagueDayId` (rejects identical home==away, lifecycle-guarded by existing `assertFixtureMutable`).

- **BSL admin editability** (League Control + AdminLayout + Competition Rules): `bsl_league_days.rubbersPerFixture` (nullable int) overrides the per-category rubbers count when the day is generated — both `POST /api/bsl/admin/fixtures/generate` and `POST /api/bsl/admin/club-fixtures` resolve `dayOverride || settings.rubbersPerFixture || lineup.length`. New `PATCH /api/bsl/admin/league-days/:id` (date / status / division / category / rubbersPerFixture) lets admins fully edit a day after creation; date is always editable, structural keys refused with 409 once `state` is LIVE or CLOSED to avoid desync from snapshotted fixtures (move back to DRAFT/PUBLISHED + Regenerate). `LeagueControl.tsx` day cards now expose a datetime-local + rubbers number input that auto-save on blur, plus a "Rubbers" field on the Add-day form (blank = use category default). `PATCH /api/bsl/league` allowlist now sanitises `divisions` (trim/dedupe, ≤56 chars, max 32) — without this, Add/Rename/Delete on the Divisions panel silently no-op'd. `CompetitionSettings.tsx` `NumberInput` allows fully clearing the field while typing and clamps min/max only on blur (sets/points/etc. were snapping to 0 mid-keystroke). `AdminLayout.tsx` mobile topbar bumped to h-16 with a larger menu button (p-3, h-7 w-7 icon) + a w-3 spacer between menu and back arrow + larger back-arrow tap target (h-10 w-10) so they no longer overlap.

### Universal Notification Engine
Admin-editable templates + on/off toggle for every event-driven push.
- **Phase 1 — Core**: Table `notification_rules` (ruleKey UNIQUE, enabled, category, title, message, settings jsonb). Single source: `RULE_REGISTRY` in `server/notificationRules.ts` (~35 keys grouped: Account/Membership/Payments/Sessions/Rewards/League BSL/Tournaments/Communication/Profile). `ensureRuleSeeds()` upserts only `category` so admin edits survive. Helper `sendRulePush(ruleKey, userIds, vars, opts)` (aka `notifyEvent`) — 30s in-memory cache, short-circuits if disabled, renders `{var}` placeholders. Wired in: `server/auth.ts`, `server/routes.ts`, `server/pushScheduler.ts`, `server/bsl-routes.ts`. Admin UI `/admin/notification-rules` (`client/src/pages/admin/NotificationRules.tsx`) — collapsible category sections, per-rule Switch + Title/Message editors + dynamic placeholder chips.
- **Phase 2 — Smart Targeting**: Weekly cron `runProfileIncompleteReminder` in `server/notificationCrons.ts` scans missing `users.phone` / `playerProfiles.gender`, fires `profileIncomplete` rule deduped via `push_send_log`.
- **Phase 3 — User Preferences**: Per-user category × channel matrix in `user_notification_prefs.categoryPrefs` JSONB. Filter `getOptedInUserIdsByCategory(userIds, category, legacyKey, channel)` in `server/oneSignal.ts` honors matrix + legacy boolean back-compat. UI `/settings/notifications` (`client/src/pages/NotificationSettings.tsx`) — grid with optimistic toggles + "All" master per row.
- **Phase 4 — Scheduled Broadcasts**: Table `notification_schedules`. Cron `runScheduledNotifications` sweeps every 60s, atomically claims `pending → sending`. Endpoints `GET/POST/DELETE /api/admin/notification-schedules` (ALL segment OWNER-only). `/admin/push-broadcast` has datetime-local "Schedule for later" + upcoming list with cancel. Per-rule **Test send to me** (`POST /api/admin/notification-rules/:key/test`) + live Preview card.
- **Phase 5 — Analytics + Email**: Table `notification_send_metrics` appended per-channel inside `sendRulePush`. `GET /api/admin/notification-rules/:key/stats?days=30` aggregates per channel, surfaced in collapsible Stats panel (Push/In-app/Email tiles). Email via `server/emailSender.ts` (Resend REST, secret `RESEND_API_KEY`, `EMAIL_FROM` env). `sendEmailToUsers()` batches max 50/email. `sendRulePush` opt-in via `opts.email: true`.

### Push Notifications (OneSignal)
Web Push via OneSignal v16 SDK (script in `client/index.html`, worker `client/public/OneSignalSDKWorker.js`). Init/external-id/player-id via `client/src/components/OneSignalBootstrap.tsx` + `client/src/lib/oneSignal.ts`. Backend `server/oneSignal.ts` (REST `Key` auth on `https://api.onesignal.com/notifications`, `include_aliases.external_id`). Routes `server/notificationRoutes.ts`: `POST /api/notifications/register`, `GET/PATCH /api/notifications/preferences`, OWNER/ADMIN `POST /api/admin/notifications/send` (segments USER/CLUB/TEAM/TOURNAMENT/ALL — ALL OWNER-only). Hourly cron `runPostSessionUnpaidReminder` in `server/pushScheduler.ts`. Tables: `user_push_subscriptions`, `user_notification_prefs`, `push_send_log`. Secrets: `VITE_ONESIGNAL_APP_ID`, `ONESIGNAL_REST_API_KEY`.

## Product

- **Club Management**: Multi-club, memberships, recurring events, sessions, financial intelligence.
- **Player Engagement**: Rankings, match organization, profiles, badges, AI analytics.
- **Admin Tools**: User/club/venue/admin management, audit logs, helpdesk, OWNER-only Control Center.
- **Advanced**: AI reporting, schedule generation, 3D avatars, league, tournaments, merchandise, community hub.
- **Monetization**: Freemium, tiered fees, credit requests, club-scoped referrals.

## User preferences

Preferred communication style: Simple, everyday language.

- **Multi-role users**: `users.secondaryRoles text[] DEFAULT '{}'` lets a single user hold multiple platform roles at once (e.g. an OWNER who is also a COACH, or an ADMIN who is also an ORGANISER). Primary `role` stays single-valued + drives the role badge; `secondaryRoles` is an additive list that grants additional access. **Coach grant flow** (`POST /api/admin/users/:id/grant-coach` in `server/coachBookingRoutes.ts`): if user is `PLAYER`, primary role is upgraded to `COACH`; otherwise `COACH` is appended to `secondaryRoles` so OWNER/ADMIN/ORGANISER status is preserved. Revoke does the inverse. Sidebar "Coach Dashboard" entry checks `user.role === "COACH" || (user.secondaryRoles ?? []).includes("COACH") || isAdminOrOwner`. Admin role editor (`/super-admin` Users panel in `client/src/pages/admin/ClubManagement.tsx`): primary role select + per-user "Extra roles" chip toggles (OWNER/ADMIN/ORGANISER/COACH). `PATCH /api/admin/users/:id/role` (OWNER-only) accepts `{role?, secondaryRoles?}` — server filters secondaryRoles against allowed enum + drops any value equal to the new primary role to prevent dupes.

- **Training Challenges for All Users** (`/training-challenges`, `client/src/pages/TrainingChallenges.tsx`): Reuses exported `ExerciseChallengePanel` from `client/src/pages/Juniors.tsx`, passes current user as synthetic single-element `juniors` array so completions write against `juniorChallengeCompletions.userId = user.id`. Read-only GETs (`/api/junior-exercises`, `/api/junior-weekly-challenges`, `/api/junior-skill-points/:userId`, `/api/junior-exercise-videos`) opened by removing `requirePremium`; POST/PATCH/DELETE remain premium. **IDOR-safe**: `GET/POST /api/junior-challenge-completions*` and `GET /api/junior-skill-points/:userId` enforce self / parent-of-child (via `users.parentUserId`) / OWNER-ADMIN only. Panel accepts `isSelfView` prop to flip "your child" → "you/your" copy. Sidebar entry "Training Challenges" in Activity group.

- **User-Editable Low-Balance Alert + Premium Membership Payment Method**: Users set wallet alert via `/profile` → Credit Wallet & History modal. Backend `PATCH /api/my-wallets/:walletId/threshold` (wallet-owner-only) writes `wallets.lowBalanceThreshold` — same column the admin UI uses, so views stay in sync. Admin "Mark Paid" on `/admin/membership-board` opens payment-method dialog: **External bank transfer** (just `paymentConfirmed=true`) OR **Member's wallet credit** (atomic deduction from `credit_ledger` + mirrored DEBIT into `wallets.balance`/`wallet_transactions` inside `db.transaction` with `SELECT ... FOR UPDATE` lock — same lockstep as god-mode `set-balance`). Price: `clubMemberships.proratedPrice ?? membershipPlans.annualPrice`. Insufficient credit returns 400 with details. Wallet option auto-disables when price=0. `PATCH /api/club-memberships/:id/payment` accepts optional `paymentMethod: "external" | "wallet"`. Bulk `mark_paid` external-only.

- **Session Financial Snapshot** (`client/src/components/financial/SessionFinancialSnapshot.tsx`): Per-session "View Snapshot" button next to delete on each session card in `/admin/financials` opens mobile-first dialog: header (title, club, venue, date, time, **duration**, **coach**), summary tiles, attendees with color-coded payment-status badges, expenses (informational only), and **Coach Earnings = total session fee (full expected income), NOT collected and NOT minus expenses** (coach pays expenses upfront and is owed the full session fee regardless of collection). Screenshot mode toggle + PNG export (html2canvas) + multi-page PDF export (jsPDF, slices canvas at A4 page height) + Web Share API with download fallback. Backed by `GET /api/sessions/:id` (returns session + venue + creator + **`coachUser`** hydrated from `sessions.coachUserId` with creator fallback, gated via `canPerform("VIEW_CLUB", session.clubId)` — never returns creator email).

- **Admin Wallet Unified View + Manual Amend** (`/super-admin/wallet-management`, `client/src/pages/super-admin/WalletManagement.tsx`): "Log" button opens modal mirroring user view (UNION of `credit_ledger` + `player_reward_ledger`, same as `/api/my-credits*`). Three cards: User sees / Wallet table balance / Drift (with amber warning if non-zero). Per-club balance chips, history (200 rows), "Set exact balance" form → `POST /api/god-mode/wallets/:walletId/set-balance` `{clubId, targetBalance (pence), reason?}`. Endpoint computes `delta = target − currentLedgerSum` and atomically writes corrective row to BOTH `credit_ledger` AND `wallets.balance`/`wallet_transactions`. Read: `GET /api/god-mode/wallets/:walletId/unified-view`. Both OWNER/ADMIN-only with `[AUDIT]` log lines.

## My Training Profile (player-facing)
`/my-training-profile` (`client/src/pages/MyTrainingProfile.tsx`) — Novos.gg-inspired esports skill-score dashboard. Layout: outer dark-purple/fuchsia gradient card with "YOUR SKILL SCORE" header, then a 2-column grid `[260px left rail | main]`. **Left rail**: avatar + "Hey {fullName}" greeting + numbered category list (each row = step number circle + category name; circle turns emerald with check when avg ≥90%, fuchsia glow when active); rail background uses the active category's mood photo (`imageForCategory()` keyword mapper, 12 AI images in `client/src/assets/skills/*.png` covering Serving/Forecourt/Midcourt/Rearcourt/Footwork/Grip/Deception/Tactical/Doubles/Fitness/Mental + default) blended over a gradient at multiply, plus a 3-tile foot row (Assessed / 5-Star / Priority counts). **Main**: header chips (`Your Score VS Potential`) + active category badge, then 2-col grid: (1) `ScoreMeter` SVG dual-ring (current emerald-cyan ring + dashed potential rose-fuchsia ring; potential = current + 45% closure to 100%) with Now/After-Coaching tiles; (2) Recharts `RadarChart` per-skill comparison (current emerald fill vs potential rose dashed). Below = mini list of every skill in the active category (tap → detail dialog) showing priority Zap, comment icon, name, star row, %, and gradient progress bar. Detail `Dialog` keeps the original gradient header with category photo at 50% opacity, animated progress bar, star rating row, priority callout, coach-comment block. **Empty state** (`isFullyEmpty` = no skills configured OR none assessed): replaces main panel with `EmptyAskCoach` — Trophy in glowing fuchsia disc, "No skills assessed yet" headline, copy explaining the coach hasn't rated their skills, plus "Ask a coach for feedback" button → `/coaching?tab=find`. Read-only — no PATCH endpoints. Reads `(user as any).playerProfiles[]` (filters `membershipStatus === "APPROVED"`), club picker (top-right header) shown when user belongs to >1 approved club. Hits existing `GET /api/players/skill-categories?clubId=`, `/api/players/skills?clubId=`, `/api/player-skills/progress/:playerId` (self-authorises via `isOwnProfile` against `playerProfiles.userId`). Default category set in `useEffect` (never during render).

## Coaching hub tabs
`/coaching` (`client/src/pages/Coaching.tsx`) is the single sidebar entry that consolidates: **Find a Coach**, **My Lessons**, **My Training Profile**, and (coach-only) **Coach Dashboard**. All four are lazy-loaded and rendered inside a `Tabs` strip (URL-driven via `?tab=`). The standalone sidebar entries for `My Training Profile` and `Coach Dashboard` were removed — Coaching is now the only entry point. Routes `/my-training-profile` and `/coach-dashboard` still exist in `App.tsx` for direct deep-links and back-compat. `isCoachish = COACH || OWNER || ADMIN || secondaryRoles.includes("COACH")` gates the Coach Dashboard tab. TabsList wraps on mobile (`flex-wrap gap-1 h-auto`).

## Dashboard hero
13-tile uniform 3-per-row hero in `client/src/components/dashboard/DashboardHero.tsx`. Reusable `Tile` shell (gradient + 2 ambient blur halos + min-h 180px). Tiles: (1) Greeting+clock with `users.profilePictureUrl` round avatar (gradient-initial fallback) + time-of-day accent palette switch (morning amber/orange, afternoon sky/cyan, evening fuchsia/violet, night indigo) + greeting word, (2) Weather (Open-Meteo geolocated, fallback Birmingham), (3) At-a-glance counters (today/this-week + countdown to next), (4) Week strip 7 chips, (5) Up-next single session, (6) Training challenges (`/api/junior-weekly-challenges` filtered to current ISO week, 7-day dot strip), (7) Live courts (sessions where now is between start and end+90min), (8) **Today's deal** — AI-sourced live UK badminton/racket-sports deals from the open web, rotates every 6s, links out to real retailer URLs (`GET /api/daily-content/deals`), (9) Pro tip (10-tip pool, day-of-year), (10) Hydration tracker (localStorage `cm-hydration-YYYY-MM-DD`, animated SVG bottle fill, +/− cups, 8-cup goal), (11) Daily mindset quote (`GET /api/daily-content/quote`), (12) Daily poll (`GET /api/daily-content/poll` + `POST /api/daily-content/poll/vote {optionIndex}`, single-vote-per-day, percentage bars), (13) Next event (`/api/team-events` filtered status=UPCOMING + future date). **AI daily content** in `server/dailyContent.ts` — module-level Map cache keyed by `YYYY-MM-DD`, generates via OpenAI (`gpt-5.1`, `response_format: json_object`) with hard JSON-string fallback arrays (FALLBACK_QUOTES, FALLBACK_POLLS, FALLBACK_DEALS) when key missing or call fails. **Deals path** uses the OpenAI **Responses API** (`POST /v1/responses` with `tools:[{type:"web_search"}]`) for real-time scraped UK badminton/racket retailer offers — parses JSON object out of `output_text`, validates each row (brand/offer/url/category, http-only URLs, length-clamped). Falls back to chat-completions (knowledge-only real brand list) then to FALLBACK_DEALS. Single web-search call per day per server instance. Poll votes kept in in-memory `Map<date, Map<optionIndex, Set<userId>>>` (resets on server restart — acceptable for low-stakes social content). Routes wired in `server/routes.ts` via `registerDailyContentRoutes(app)`.
`client/src/components/dashboard/DashboardHero.tsx` is a 3-column hero card strip rendered at the top of `Dashboard.tsx` (just inside the `<>` after the page header, before tournament banners). Cards: (1) gradient violet/cyan greeting card with live `HH:mm` + `ss` clock (1s interval) + today/this-week session counts; (2) sky-toned weather card using browser geolocation → fallback Birmingham (52.4862,-1.8904) wired to free Open-Meteo `https://api.open-meteo.com/v1/forecast` (no API key) — shows temp, WMO-mapped icon+label, wind, humidity; (3) emerald weekly-timeline card with Mon-Sun day chips (today highlighted, count badge per day) + "Up next" list of 3 nearest future sessions linking to `/sessions/:id`. Pulls `sessions` from existing `DashboardContent` prop — additive, no other Dashboard refactor.

## Premium tile (global Card upgrade)
`client/src/index.css` defines `.premium-tile` — theme-token-aware glassy gradient (`hsl(var(--primary)/0.10)` → `hsl(var(--accent)/0.05)` → `hsl(var(--card)/0.92)` over `hsl(var(--card))`), 1px translucent border, soft drop + inset shadow, and two ambient blob pseudo-elements (top-right primary halo + bottom-left accent halo, both `blur(56-64px)`). `isolation: isolate` + `z-index: -1` on `::before`/`::after` keeps content above the halos without breaking absolutely-positioned children. `client/src/components/ui/card.tsx` `Card` now ships `"shadcn-card premium-tile text-card-foreground"` as the default class — every shadcn `<Card>` across the app (dialogs, dashboards, profiles, BSL panels, login, etc.) automatically inherits the hero-card aesthetic and adapts to whichever of the 64 themes is active. Opt-out class `.premium-tile-flat` (e.g. `<Card className="premium-tile-flat">`) restores the flat card surface for chart embeds or print contexts.

## Sidebar layout

- `client/src/components/layout/Sidebar.tsx` `collapseToHubs()` collapses `activity`, `club+design`, `comms+info` into single hub links pointing at `/hub/*`. Pinned passthrough hrefs (`pinnedActivityHrefs = ["/sessions"]`) stay visible at the top of their hub group in the sidebar AND continue to render as tiles inside the hub page (`Hub.tsx` still iterates the original `activity` group).
- Admin sidebar (`useNavGroups`) intentionally short: **Admin Panel**, **Financials**, **Admin Inbox** only. Club Control, Push Broadcast, Auto Reminders are surfaced as tiles inside `/admin` via `adminToolsSections` / `clubSettingsSections` in `client/src/pages/admin/AdminDashboard.tsx`. The `adminInbox` badge piggy-backs on the **Admin Inbox** entry.

- **Team Members on Sessions — Multi-Assignment** (`/admin/clubs` → Members tab; Sessions create/edit; Timeline + Calendar views on `/sessions`): `playerProfiles.teamRoles text[]` stores per-club role tags. Canonical values: `COACH`, `SUPPORT_COACH`, `ORGANISER`, `COORDINATOR`, or `CUSTOM:<Display Name>` (label 1-56 chars). Edit via `TeamRolesEditor` in `client/src/pages/admin/ClubManagement.tsx` (Popover with Checkbox list + custom input chip; PATCH `/api/clubs/:id/members/:profileId` `{teamRoles}`, server-validated against the canonical set). **Sessions support multiple users per role** via four `int[]` columns: `coachUserIds`, `organiserUserIds`, `coordinatorUserIds`, `supportCoachUserIds` (defaulting to `'{}'`). Legacy single-id columns (`coachUserId`, `organiserUserId`, `coordinatorUserId`) are kept for back-compat reads and mirrored to the first array element on PATCH. `MemberSelector` (`client/src/components/session/MemberSelector.tsx`) is now a multi-select chip picker: `values: number[]` + `onChange: (ids: number[]) => void`. CreateSessionDialog + EditSessionDialog render four selectors (Coordinators / Organisers / Coaches / Support Coaches). Server `PATCH /api/sessions/:id` validates each user in each array is an APPROVED member of the **effective** target club via `storage.getPlayerProfile`; legacy single-id keys still accepted and converted to arrays. `GET /api/clubs/:clubId/members` is sanitized to least-privilege fields. `storage.getSessions` + `getSession` hydrate `coachUsers/organiserUsers/coordinatorUsers/supportCoachUsers` arrays (`{id, fullName}`) via a single batched `users` lookup (no N+1) and also expose `coachUser/organiserUser/coordinatorUser` (first element) for back-compat. `SessionTeamBadges` accepts both single-person and array props and renders one chip per assignee — Coordinator (gold/Crown) > Organiser (blue/ShieldCheck) > Coach (violet/GraduationCap) > Support Coach (emerald/Sparkles). Schema picks new columns up automatically via `createInsertSchema` so `POST /api/sessions` accepts them with no extra server code. Snapshot dialog joins all coaches with commas plus `(+N support)` count.

- **Sessions page views**: `/sessions` (`client/src/pages/Sessions.tsx`) only exposes **Timeline** + **Calendar** view modes. The legacy "Cards" and "Grouped" views were removed for both regular and juniors scopes. `viewMode` localStorage key `sessionsViewMode` is migration-safe (falls back to `timeline` if it finds an old `cards`/`grouped` value). All session metadata that previously lived in the Cards view (team badges, banners, allowed grades, etc.) is rendered in the Timeline card via `TimelineSessionCard`.

### Coaching hub
Find a Coach + My Lessons + Coach Dashboard live behind one sidebar entry **"Coaching"** at `/coaching` (`client/src/pages/Coaching.tsx`) — shadcn Tabs with `?tab=find|lessons|dashboard`, dashboard tab only renders for COACH/secondaryRole COACH/OWNER/ADMIN. Legacy routes `/find-coach`, `/my-lessons`, `/coach-dashboard` stay routable for deep links + back-compat. `CoachSubNav` returns `null` on `/coaching` to avoid double-nav. Sidebar consolidation in `Sidebar.tsx` (single `{ href:"/coaching", label:"Coaching", icon:GraduationCap, group:"activity" }`) auto-collapses into the Activity hub tile via `useNavGroups`.

`CoachDetailDialog` in `FindCoach.tsx` now renders a **Player Feedback** Fortnite-locker grid (violet glow for evaluations, cyan glow for coach notes) backed by `GET /api/coaches/:id/feedback` (`server/routes.ts`). Endpoint **requires authentication** and fully anonymises players via in-response label map (`Player A`, `Player B`, …) — no first names, no last initials, no PII. Joins `playerSkillEvaluations` + `playerCoachNotes` via `coach.userId`, returns up to 30 items sorted desc, filters empty comments.

### Find a Coach v2 (Booksy-style booking)
- **Tables** (in `shared/schema.ts`): `coach_availability_rules` (weekly recurring HH:MM windows by dayOfWeek 0-6), `coach_availability_overrides` (per-date `isClosed` block OR custom hours, optional note), `coach_booking_settings` (UNIQUE coachId — `slotDurationMinutes`/`bufferBefore/After`/`advanceNoticeHours`/`maxAdvanceDays`/`holidayMode`+`holidayMessage`/`defaultPricePence`/`autoApprove`), `coach_gallery_images` (sortable image URLs). Also: `lesson_request_status` enum got `NO_SHOW`; `lesson_requests` got `start_time`/`end_time` timestamptz cols. Migration via psql `ALTER TABLE … ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS` (npm run db:push hangs on new tables).
- **Backend** (`server/coachBookingRoutes.ts`, mounted from `server/routes.ts` near `registerBslRoutes`): `GET/POST/DELETE /api/coach-bookings/availability/rules`, `GET/POST/DELETE /api/coach-bookings/availability/overrides`, `GET/PUT /api/coach-bookings/settings`, `GET/POST/DELETE /api/coach-bookings/gallery/(me|upload|:id)` (multer 6MB → `public/uploads/coaches/gallery/`), `GET /api/coaches/:id/availability-summary` + `GET /api/coaches/:id/availability-slots?date=YYYY-MM-DD` (slot generator: respects rules + overrides + booking buffers + advance-notice + max-advance + holidayMode + conflicts vs PENDING/ACCEPTED `lesson_requests`), `POST /api/coach-bookings` (player creates — verifies slot still free, optionally auto-accepts), `GET /api/coach-bookings/coach|player`, `PATCH /api/coach-bookings/:id/status {status: APPROVED|REJECTED|COMPLETED|NO_SHOW|CANCELLED, coachResponse?, agreedPrice?}` (APPROVED→ACCEPTED, REJECTED→DECLINED to fit existing enum), `PATCH /api/coach-bookings/:id/cancel` (player). Admin: `POST /api/admin/users/:id/grant-coach` (sets role=COACH, ensures coach row APPROVED, seeds default booking settings, fires `coachRoleGranted` rule), `POST /api/admin/users/:id/revoke-coach`, `GET /api/admin/users/search-for-coach?q=`.
- **Notification rules** (Coaching category, in `server/notificationRules.ts` `RULE_REGISTRY`): `coachRoleGranted`, `coachLessonRequested`, `coachLessonApproved`, `coachLessonRejected`, `coachLessonCompleted`, `coachLessonNoShow`, `coachLessonReminder` — fully editable from `/admin/notification-rules`.
- **Frontend**:
  - `/coach-dashboard` (`client/src/pages/CoachDashboard.tsx`, sidebar entry visible to COACH + ADMIN/OWNER): 5 glass-morph tabs (Bookings/Availability/Overrides/Settings/Gallery) with framer-motion hero strip, respond modal (Approve/Reject + message + agreed-price), per-day weekly slot manager, blocked-date editor, holiday-mode toggle, gallery uploader.
  - `/coach/:id` (`client/src/pages/CoachPublicProfile.tsx`): hero (avatar with violet/fuchsia/cyan glow ring + BE Cert / First Aid / years badges), gallery carousel, qualifications + achievements + philosophy cards, **interactive booking calendar**: 14-day horizontal date strip → slot grid (booked/too-soon greyed) → expanding form (duration/type/location/message) → POST.
  - `/find-coach`: each coach card now has a "Book" button linking to `/coach/:id` (existing dialog still opens on body click).
  - `/admin/coach-management` "Coaches" tab gained a **Promote user to coach** dialog (search by name/email, one-click grant).
- **Coach profile extras + venue-locked booking** (May 2026): `coaches` table extended with `services_description text`, `video_links text[]`, `website_links text[]`, `preferred_venue_ids integer[]`, `preferred_areas text[]` (defaults `'{}'`, mirrored in `shared/schema.ts`). `PATCH /api/coaches/me` allowlist accepts these + `profilePhoto` URL/path; URL-list sanitisers enforce https only (≤12 entries), areas ≤20×80 chars, venue IDs positive ints (≤50). New `GET /api/venues/all` (auth-required) returns `{id,name,address,city,postcode,googleMapsUrl,clubId,clubName}` from `venues + clubs leftJoin`, sorted by name — the single source of truth for both the coach's preferred-venue picker and the player's booking dropdown. `CoachProfile.tsx` edit form gained: photo URL input next to Upload Photo button, Preferred Venues checkbox list (filtered from `/api/venues/all`), Preferred Areas textarea (lines), and a "Services, Links & Videos" section (servicesDescription textarea + websiteLinks/videoLinks line-textareas with shared `linesToList`/`listToLines` helpers). View mode renders all five fields when populated. `CoachPublicProfile.tsx` booking form replaces the free-text "Preferred location" `Input` with a `Select` of the coach's `preferredVenueIds` (resolved against `/api/venues/all`) PLUS a separate "Suggest a different venue (optional)" text input — the POST body composes `location` as `"VenueName, City"` (+ `" (Suggestion: ...)"` when provided), so the coach decides the final venue. Empty preferred-venues list shows an amber notice and exposes only the suggestion box. Public profile also surfaces a Services / Areas / Website / Videos card above the booking pane.
- **Map**: still Leaflet OpenStreetMap. When user adds `VITE_MAPBOX_TOKEN`, swap tile layer in FindCoach to Mapbox dark + add `mapbox-gl` clustering.
- **Coach Lesson Packages + Payouts** (May 2026): `coach_booking_settings.price_tiers jsonb DEFAULT '[]'` stores an editable list of packages `{id,label,pricePence,durationMinutes,maxParticipants,sortOrder}` (max 24, server-sanitised + clamped in `PUT /api/coach-bookings/settings`). `GET /api/coaches/:id/availability-summary` returns `priceTiers` so booking surfaces can render them. `POST /api/coach-bookings` accepts optional `priceTierId` — when supplied the server resolves duration + agreedPrice from the tier (max-participants > 1 forces `lessonType=GROUP`), else falls back to client-supplied values. `CoachDashboard.tsx` Settings tab gained a **Lesson packages** editor with preset chips (1-to-1 / 1-to-2 / Group / Custom) + per-row label/price£/duration/maxParticipants/delete. Booking surfaces (`FindCoach.tsx` RequestLessonDialog + `CoachPublicProfile.tsx`) replaced the duration/type Selects with a tier-chip picker that drives the live price + duration summary; selection is optional (falls back to coach defaults). New endpoint `GET /api/coach-bookings/payout-info` returns `{platformFeePct:3, payoutSlaHours:48, payoutMessage, bankNote, clubBanks[]}` aggregated from `coaches.linkedClubIds` (account number masked to last-4 client-side). Coach dashboard renders a `PayoutPanel` with the emerald 3% / 48-hour callout and a read-only club bank list. Bank details remain OWNER-only via existing `PATCH /api/clubs/:id` allowlist in `server/routes.ts:14810` — coaches cannot edit them.

## Gotchas

- Express JSON body limit is path-aware in `server/index.ts`: default 256kb, with `/api/bsl/clubs*` opted into 8mb for base64 image fallback. Extend `jsonLargeRoutes` to raise per-route — never globally.
- Merchandise Supplier Order Sheet excludes customer contact details (email, phone) for privacy.

## Pointers

- **TanStack React Query**: https://tanstack.com/query/latest
- **Drizzle ORM**: https://orm.drizzle.team/
- **Tailwind CSS**: https://tailwindcss.com/
- **shadcn/ui**: https://ui.shadcn.com/
- **framer-motion**: https://www.framer.com/motion/
- **Vite**: https://vitejs.dev/
- **esbuild**: https://esbuild.github.io/
- **OpenStreetMap Nominatim**: https://nominatim.org/release-docs/latest/api/Search/
- **Google Calendar API**: https://developers.google.com/calendar/api
