# Club Master

Comprehensive web app for racket-sports clubs (operations + player engagement).

> Full historical detail for every module below lives in **`docs/replit-history.md`** — this file is a leaner index. Open the archive when you need full implementation prose.

## Stack

- **Frontend**: React 18, TypeScript, Wouter, TanStack React Query, Tailwind CSS, shadcn/ui
- **Backend**: Node.js, Express.js, TypeScript
- **Database**: PostgreSQL with Drizzle ORM
- **Build Tools**: Vite (frontend), esbuild (backend)
- **Other**: express-session, passport/passport-local, date-fns, recharts, framer-motion

## Where things live

- `client/` — Frontend (`client/src/index.css` = base CSS tokens; `client/src/hooks/use-theme.ts` = theme classes)
- `server/` — Backend
- `shared/schema.ts` — Drizzle schema (single source of truth)
- `docs/replit-history.md` — Full module-by-module history

## Architecture decisions

- **Theming**: 64-theme Premium system + global Ultra-Premium Glass UI. Default "Dribbble Blue-Violet" in `client/src/index.css` `:root`/`.dark` (violet accent `252 90% 68%`). Legacy bypass: `Dashboard.tsx`, `Deals.tsx`, `MerchandisePage.tsx`, `not-found.tsx`.
- **Matchmaking**: Smart Match Engine v6 (gender-aware, 9-tier grade scoring) + BPG Competitive Balance + Session Fairness Command Center. **v6.1 fixes (May 2026)**: (a) `buildPairingHistory` now also returns `recentGroups` (Map of sorted-id-joined 4-player keys → count); `GenerateOptions.recentGroups` seeds `localGroupHistory` so foursomes don't repeat across the per-match invocation pattern. (b) `selectUnits()` early-exits at 1500 groups during generation (previously materialised all C(N,4) before slicing — O(N⁴) in singles count). (c) `applyAIBrainLayer` now builds a single `playerMap` (O(1) lookup) and replaces `qualifiedMatches.indexOf(a)` inside the comparator with a `qualityByMatch: Map<MatchResult, number>` (was O(N²·N)). `computeMatchQualityScore`/`computeAIScore` accept the map via optional `playerMapArg`. (d) `tryGenerate` fallback in `generateNextMatch` now `Math.max(hardGradeSpreadLimit, primarySpread+1)` so the fallback always RELAXES (was tighter than primary with default settings). (e) `shouldBlockMixedForMale` now requires literally 3 mixed in last 3 (was 2-of-3 with impossible men-doubles secondary clause that locked players). (f) DEFAULT_SETTINGS tightened: `gradeSpreadLimit 5→3`, `hardGradeSpreadLimit 4→5` (now correctly looser than primary), `teamAvgDiffLimit 3→1.5`. Per-club `matchEngineSettings` overrides preserved.
- **Freemium**: 2-plan (Basic FREE / Premium), backend-enforced + frontend-gated.
- **Multi-Tenancy**: Multi-club + multi-sport, role-based.
- **Club Control Center** (`/admin/control-center` → `client/src/pages/admin/ClubControlCenter.tsx`): OWNER sees all clubs (`/api/super-admin/clubs/billing`); ADMIN sees own. Per-club feature toggles in `clubs.featureOverrides` jsonb (`GET/PATCH /api/clubs/:id/feature-overrides`, PATCH = OWNER-only).
- **Localization**: All event date/time anchored to `Europe/London`.
- **Player Rankings UI** (`client/src/pages/PlayerRankings.tsx`): Top-3 = `HeroPlayerCard`, ranks 4+ = `CompactPlayerCard`. All surfaces use `hsl(var(--card)/x)` tokens.

## Module index (see `docs/replit-history.md` for full detail)

### Birmingham Super League (BSL) — `/bsl`
Esports-style competition module. Locked palette: bg `222 50% 6%`, cyan `195 100% 60%`, gold `42 95% 55%`. Backend: `server/bsl-routes.ts`. Schema in `shared/schema.ts` (5 enums + ~14 tables under `bsl_*`).

- **Public routes**: `/bsl` (LeagueMode hub), `/bsl/register-club`, `/bsl/join`, `/bsl/wallet`, `/bsl/match/:id`, `/bsl/profile` (player), `/bsl/my-club` (manager), `/bsl/prizes`.
- **Admin Control Panel** (`/bsl/admin/*`, OWNER/ADMIN, sidebar "BSL · Control Panel"): Dashboard, league control, match-day live + match-days hub, clubs, players, payments, media, settings, prizes, competition rules. Audit via `audit()` helper → `bsl_audit_log`.
- **Share invite (link + QR)**: `client/src/pages/bsl/components/ShareInviteDialog.tsx` — Copy QR (real PNG via `ClipboardItem`), Save PNG, Web Share. Wired in LeagueMode/ClubManager/ClubsAdmin. `JoinPlayer.tsx` auto-validates `?code=`. `PrivateRoute` bounces logged-out users to `/login?next=…` (sanitised by `safeNext()` — internal `/`-prefix only).
- **Club-vs-Club fixtures + drag-drop pair assignment**: `bsl_fixtures.home/away_club_id` (legacy team_id nullable). `POST /api/bsl/admin/club-fixtures` creates fixture + 6 default rubbers `[MD,MD,WD,WD,XD,XD]`. `PATCH /api/bsl/admin/rubbers/:id/assign` validates club + category. UI `client/src/pages/bsl/admin/FixtureSetup.tsx` (3-col board, HTML5 DnD + select fallback). Add/Trash chips, auto-fill modes `vs_all` (Cartesian) / `parallel` (index-by-index). All endpoints lifecycle-guarded via `assertFixtureMutable`.
- **Category Competition Settings + snapshot-on-generate** (`/bsl/admin/competition`): Replaces hardcoded lineup/sets/points. Tables `bsl_category_settings` (per leagueId+category — rubbersPerFixture/rubberLineup[]/setsPerMatch/pointsPerSet/scoringRule/deuceCap/format/courtPool/walkoverPolicy/tiebreakOrder[]/pointsWin·Draw·Loss), `bsl_fixture_versions` (archive jsonb per Regenerate). Lifecycle cols: `bsl_league_days.state` (DRAFT|PUBLISHED|LIVE|CLOSED) + division/category; `bsl_fixtures.category/version/rulesSnapshot/walkoverWinner/liveStartedAt/livePausedAt`; `bsl_rubbers.setScores jsonb + walkoverWinner`. Endpoints: `GET/PUT/DELETE /api/bsl/admin/category-settings[/:cat]`, `POST /api/bsl/admin/fixtures/regenerate` (archives SCHEDULED/WARMUP only, rebuilds at version+1, LIVE/FINISHED preserved), `GET /api/bsl/admin/fixture-versions`, `PATCH /api/bsl/admin/league-days/:id/state` (LIVE blocked when 0 fixtures; CLOSED triggers `recomputeStandings`), `POST /api/bsl/admin/fixtures/:id/live {action}`, `POST /api/bsl/admin/rubbers/:id/walkover {winner}`. `recomputeStandings` reads pointsWin/Draw/Loss from fixture snapshot first.
- **Wallet top-up — category packages + multi-select discounts** (`/bsl/wallet`): admin-defined packages (`bsl_leagues.topup_packages jsonb` = `[{id,label,amountPence,sortOrder}]`, max 24) with a configurable discount ladder (`bsl_leagues.topup_discount_pcts jsonb` default `[0,50,70]` = 1st full, 2nd 50% off, 3rd 70% off, 4th+ full). Admin UI at `/bsl/admin/settings` "Wallet Top-Up Packages" panel (label + £ amount + Restart row + per-rank % editor). Player presses package buttons — each click appends to `clickHistory[]`, shows ×qty badge, and live-recomputes total via `shared/topupPricing.ts` (`computeTopup(clickHistory, packages, discountPcts, customPence)`). Repeats follow click-rank order so 3× Adult = full + 50% + 70%. Custom amount field never gets a discount. Restart button clears history+custom. Server (`POST /api/bsl/wallet/topup`) accepts `clickHistory` JSON + `customAmountPence`, **always recomputes via the same engine** (never trusts client total), writes a human-readable description (`2× Adult · 1× Junior · (−£X.XX discount)`). Legacy `amount` field still accepted as fallback.
- **Club lifecycle controls — Sleep / Wipe** (`/bsl/admin/clubs`, OWNER-only): `bsl_clubs.sleeping_at timestamp` (nullable). `PATCH /api/bsl/admin/clubs/:id/sleep {sleeping:bool}` toggles archive flag — all data preserved, public LeagueMode tile + admin row show "Sleeping" badge. `DELETE /api/bsl/admin/clubs/:id/wipe {confirmName}` hard-deletes inside `db.transaction`: deletes `bsl_fixtures` where home/away matches (rubbers cascade), then the club row (teams cascade, players SET NULL — wallet history preserved with the player). Confirmation phrase MUST equal the exact club name. Audit `club.sleep|wake|wipe`.
- **Super-admin club/player controls** (`/bsl/admin/clubs|players|clubs/:id/manage`, ~lines 806-1060 in `bsl-routes.ts`): create clubs/players outside the public wizard, force-activate, wallet adjust (atomic `db.transaction`, properly-typed `TOPUP`/`DEDUCTION`), categories override (no fee), `GET /api/bsl/admin/clubs/:id/manager-view`. **Wallet enum**: `TOPUP | DEDUCTION` (NEVER `DEBIT`).
- **Club Manager Dashboard** (`/bsl/my-club`): 6-tile strip + Members table. `loadClubForManager()` helper. Pairs in `bsl_team_members` (max 2, auto-removes from sibling pair). `PATCH /api/bsl/clubs/:clubId/players/:playerId` edits displayName/bio.
- **Player Profile Dashboard** (`/bsl/profile`): single round-trip `GET /api/bsl/players/me/dashboard`. Per-fixture home/away derived from rubber slot (prevents WIN/LOSS inversion). Categories register/unregister via `POST/DELETE /api/bsl/players/me/categories` — atomic conditional UPDATE with SQL `CASE` for tier pricing (1st full, 2nd 50% off, 3rd 70% off), keyed off `array_length(categories,1)`. Per-cat fees in `bslLeagues.categoryFees` jsonb (defaults `{MD:2500,WD:2500,XD:3000}` pence).
- **Year-End Prize Vault** (public `/bsl/prizes`, admin `/bsl/admin/prizes`): Tier cards (DIAMOND/PLATINUM/GOLD/SILVER/BRONZE/MYTHIC/EPIC). Table `bsl_prizes`. `POST /api/bsl/admin/prizes/seed {replace?}`.
- **Match Days hub** (`/bsl/admin/match-days`): card grid + `MatchDayEditor` modal (date/time/venue/division/category/rubbers/lifecycle + inline fixtures table). `GET /api/bsl/admin/league-days/:id/details`. New cols `bsl_league_days.venue + notes` (240/2000 char caps). `PATCH /api/bsl/fixtures/:id` extended for `homeClubId/awayClubId/bslLeagueDayId`.
- **Admin editability**: `bsl_league_days.rubbersPerFixture` (nullable) overrides per-day. `PATCH /api/bsl/admin/league-days/:id` (date always editable; structural keys 409 once LIVE/CLOSED). `PATCH /api/bsl/league` allowlist sanitises `divisions[]` (trim/dedupe, ≤56 chars, max 32).
- **NOTE**: `npm run db:push` may hang on interactive prompts for new tables — use `psql "$DATABASE_URL"` with `ALTER TABLE … ADD COLUMN IF NOT EXISTS` / `CREATE TABLE IF NOT EXISTS`.
- **Multi-division clubs + correct counts** — `bsl_clubs.additionalDivisions text[] NOT NULL DEFAULT '{}'`. A club has a primary `division` plus optional extras (≤8, server-sanitised: trim/dedupe/drop primary). `POST/PATCH /api/bsl/admin/clubs` accept `additionalDivisions`. `LeagueControl` divisions panel now counts CLUBS per division (was incorrectly summing `teamCount` against primary `division` only — under-counted multi-div clubs and showed "0 teams" for divisions populated entirely by clubs whose primary was elsewhere). Each club counts once per division it participates in (primary ∪ additional). `ClubsAdmin` editor + create dialog expose chip-toggle additional-divisions picker; clubs table shows `+Div` cyan chips next to primary.
- **Pay-per-division joining (club manager flow)** — `bsl_leagues.division_join_fee_pence int NOT NULL DEFAULT 2500` (admin-editable in `/bsl/admin/settings` "Division join fee" panel; PATCH `/api/bsl/league` allowlist). New endpoint `POST /api/bsl/clubs/:id/join-division {division}` (manager-gated via `loadClubForManager`): atomic txn with `SELECT … FOR UPDATE` on caller's `bsl_players` row, deducts fee from player wallet, writes `bsl_wallet_transactions` `DEDUCTION` (status APPROVED, ref `BSL-DIV-…`), appends division to `bsl_clubs.additionalDivisions`, audit `CLUB_JOIN_DIVISION`. 400s on insufficient balance / non-member / already-joined / division not in `bslLeagues.divisions`. Returns `{club, feePence, balance}`. `GET /api/bsl/my-club` now includes `league: {divisions, divisionJoinFeePence}` so the UI can render the picker. Pair-create endpoint `POST /api/bsl/clubs/:id/teams` accepts `{category, division?}` (defaults to primary; validates against joined set; pair lettering is per (division, category) so MD-A in Premier ≠ MD-A in Social). **Sibling-pair scope is per (division, category)** at `~bsl-routes.ts:2582` so a player can be in MD across multiple divisions but not in two MD pairs inside the same division. `ClubManager.tsx` shows a "Divisions" panel (gold Crown chip = primary, cyan chips = extras, picker + `Join (£X.XX)` button with confirm) plus one "<div> — Pairs by category" GlowPanel per joined division (each with its own MD/WD/XD grid). Schema: declared at `shared/schema.ts` `bslLeagues.divisionJoinFeePence`. Removed unused `bsl_clubs.division_pairs` (redundant — `bsl_teams` already keys by division+category).
- **Admin user search bug-fix** — `/api/bsl/admin/users/search` now uses SQL `ILIKE` against the full `users` table (drizzle `or(ilike(fullName), ilike(email))`) ordered by `fullName ASC` with `LIMIT 50`. Previous implementation pulled `desc(users.id) LIMIT 500` then JS-filtered — older accounts (Mikey Hewitt, Pawan, etc.) silently fell off the bottom and never appeared in the "Create player" dropdown.
- **Permissions, Grading & Captains** — `bsl_leagues.player_grades jsonb` (catalogue `[{code,label,sortOrder}]`, default A1/A2/B1/B2/C1/C2/C3) + `divisionGrades jsonb` (per-division allowed-codes map; empty list = no restriction). `bsl_players.grade text` (nullable). `bsl_clubs.adminUserIds int[]` (additional club admins with same powers as owner; owner is implicit). `bsl_teams.captainPlayerId int` (FK SET NULL — per-division captain). Helper `isGradeAllowedInDivision()` enforced at `POST /api/bsl/players/join` (refuses join when destination division has any restriction) and `POST /api/bsl/admin/players` (admin create accepts `grade`). New endpoints: `POST /api/bsl/admin/divisions/rename {from,to}` (atomic transactional cascade across `bsl_clubs/bsl_teams/bsl_league_days/bsl_prizes/bsl_leagues.divisions+divisionGrades` — IDs preserved, only labels propagate); `PATCH /api/bsl/players/:id/grade {grade|null}` (super admin OR club admin/owner; validates against league catalogue); `POST /api/bsl/admin/players/:id/transfer {toBslClubId}` (super-admin only — locked when `matchesPlayed>0` OR has any `bslTeamMembers` row; clears `bslTeamId`+`confirmedByOwnerAt`; eligibility-checked); `PATCH /api/bsl/clubs/:id/admins {userIds}` (owner OR super admin; validates each is ACTIVE player of club, ≤16, owner pruned); `PATCH /api/bsl/admin/clubs/:id/owner {userId}` (OWNER-only; new owner pruned from adminUserIds); `PATCH /api/bsl/teams/:id/captain {playerId|null}` (club admin/owner OR super admin; captain must be ACTIVE player of same club). All endpoints `audit()`-logged. UI: `/bsl/admin/settings` adds "Player Grades" + "Divisions & Eligibility" panels (chip toggles + Rename modal). `/bsl/admin/players` editor adds Grade chip-picker + Transfer-to-club dropdown (disabled when `matchesPlayed>0`). `/bsl/admin/clubs` editor adds Club Admins toggle list (from manager-view roster) + Reassign Owner search box (uses existing `/api/admin/users/search-for-coach`).

### Persistent file storage (Object Storage)
All uploads (profile/coach photos+gallery, club logos, announcements, BSL media, incidents) → Replit Object Storage via `server/uploadStorage.ts` (`saveBufferToBucket(buf, prefix, originalname)` → `/files/<key>`). Multer = `memoryStorage` everywhere. `registerFileServeRoute(app)` adds `GET /files/*` (Express-5 regex route) with long-cache + content-type from extension. Legacy `/uploads/...` static fallback still mounted (vanishes on autoscale deploy). Validators accept both `/uploads/` and `/files/` prefixes. Bucket id = `DEFAULT_OBJECT_STORAGE_BUCKET_ID`.

### Universal Notification Engine
Admin-editable templates + on/off toggles for every event-driven push. Source of truth: `RULE_REGISTRY` in `server/notificationRules.ts` (~35 keys, 9 categories). Helper `sendRulePush(ruleKey, userIds, vars, opts)` (aka `notifyEvent`) — 30s in-memory cache, short-circuits if disabled, renders `{var}` placeholders.
- **Tables**: `notification_rules`, `notification_schedules`, `notification_send_metrics`, `user_notification_prefs` (categoryPrefs jsonb + `muted_rule_keys text[]`), `push_send_log`.
- **Admin UI** `/admin/notification-rules` — collapsible sections + per-rule Switch + Title/Message editor + placeholder chips + Test-send + Stats panel (Push/In-app/Email tiles via `GET /api/admin/notification-rules/:key/stats?days=30`).
- **User UI** `/settings/notifications` — category × channel matrix (optimistic toggles + master "All" per row). Per-rule mute via `POST /api/notifications/mute-rule {ruleKey, muted}` (currently exposed for `profileIncomplete` via `BellOff` pill in `NotificationBell.tsx`; on mute also bulk-deletes existing `notifications` rows of that type).
- **Crons** in `server/notificationCrons.ts` + `server/pushScheduler.ts`: weekly `runProfileIncompleteReminder` (deduped via `push_send_log`); 60s `runScheduledNotifications` sweep (atomically claims `pending → sending`); hourly `runPostSessionUnpaidReminder`.
- **Email** via `server/emailSender.ts` (Resend REST, `RESEND_API_KEY`, `EMAIL_FROM`); `sendEmailToUsers()` batches 50/email; opt-in via `opts.email: true`.

### Push Notifications (OneSignal)
Web Push via OneSignal v16 (script `client/index.html`, worker `client/public/OneSignalSDKWorker.js`). Init via `client/src/components/OneSignalBootstrap.tsx` + `client/src/lib/oneSignal.ts`. Backend `server/oneSignal.ts` (REST `Key` auth, `include_aliases.external_id`). Routes `server/notificationRoutes.ts`: `POST /api/notifications/register`, `GET/PATCH /api/notifications/preferences`, `POST /api/admin/notifications/send` (segments USER/CLUB/TEAM/TOURNAMENT/ALL — ALL OWNER-only). Tables: `user_push_subscriptions`, `user_notification_prefs`, `push_send_log`. Secrets: `VITE_ONESIGNAL_APP_ID`, `ONESIGNAL_REST_API_KEY`.

### Find a Coach v2 (Booksy-style booking)
- **Tables**: `coach_availability_rules` (weekly HH:MM by dayOfWeek 0-6), `coach_availability_overrides` (per-date `isClosed` block OR custom hours + note), `coach_booking_settings` (UNIQUE coachId — slot/buffer/advance/holidayMode/defaultPrice/autoApprove + `price_tiers jsonb`), `coach_gallery_images`. `lesson_request_status` enum gained `NO_SHOW`; `lesson_requests` got `start_time/end_time` timestamptz.
- **Backend** (`server/coachBookingRoutes.ts`, mounted near `registerBslRoutes`): availability rules/overrides CRUD, settings GET/PUT, gallery (multer 6MB → `public/uploads/coaches/gallery/`), `GET /api/coaches/:id/availability-summary|slots?date=`, `POST /api/coach-bookings` (verifies slot + optional auto-accept + `priceTierId` resolution), `GET /api/coach-bookings/coach|player`, `PATCH /api/coach-bookings/:id/status|cancel`. Admin: `POST /api/admin/users/:id/grant-coach|revoke-coach`, `GET /api/admin/users/search-for-coach?q=`. Public bulk: `GET /api/coaches/availability-month?month=YYYY-MM` (year clamped 2000-2100; **`overrides.note` stripped** since public).
- **Coach profile extras**: `coaches.services_description`, `video_links text[]`, `website_links text[]`, `preferred_venue_ids int[]`, `preferred_areas text[]`. `PATCH /api/coaches/me` allowlist + sanitisers (https only, ≤12 entries, areas ≤20×80, venue IDs ≤50). `GET /api/venues/all` is the single source for venue picker.
- **Lesson packages + payouts**: `coach_booking_settings.price_tiers jsonb` (`{id,label,pricePence,durationMinutes,maxParticipants,sortOrder}` — max 24, server-clamped). `GET /api/coach-bookings/payout-info` returns `{platformFeePct:3, payoutSlaHours:48, clubBanks[]}` (acct masked client-side last-4). Bank details remain OWNER-only via `PATCH /api/clubs/:id` allowlist.
- **Notification rules** (Coaching category): `coachRoleGranted`, `coachLessonRequested|Approved|Rejected|Completed|NoShow|Reminder`.
- **Frontend**: `/coaching` (`client/src/pages/Coaching.tsx`) consolidates Find a Coach + My Lessons + My Training Profile + Coach Dashboard behind one sidebar entry (URL-driven `?tab=`). Calendar tab `client/src/pages/CoachesAvailabilityCalendar.tsx` (month grid coloured by approved-coach count). `CoachPublicProfile.tsx` has 14-day strip → slot grid → expanding form. `CoachDetailDialog` Player Feedback grid backed by `GET /api/coaches/:id/feedback` (auth-required, fully anonymised — `Player A/B/…` map, no PII).
- **Map**: Leaflet OSM. With `VITE_MAPBOX_TOKEN` → swap to Mapbox dark + clustering.

### Super-Admin Club Finance Calculator
OWNER-only at `/super-admin/club-finance-calculator` (`client/src/pages/super-admin/ClubFinanceCalculator.tsx`). **Reached as a tile in `/super-admin/god-mode` "Super Admin Tools" section** (sidebar entry removed). Singleton row `club_finance_calculator_settings` id=1, all money in pence (membershipFee, member/nonMember session price, hall per session, **`shuttlecock_tube_price_pence` + `shuttlecock_tubes_per_session`** — total shuttle cost = price × tubes, replaces old per-session cost which is kept as legacy `shuttlecocks_cost_per_session_pence` for back-compat but unused, tshirt + memberPercentage/numberOfMembers/expectedPlayersPerSession/sessionsPerWeek/weeksPerYear + nullable `old_*` trio for comparison + updatedByUserId). Endpoints `GET/PUT /api/super-admin/club-finance-calculator` in `server/routes.ts` (~line 719) — OWNER-gated, `intField` clamps memberPct 0-100 / sess/wk 0-14 / wks/yr 0-53 / tubes 0-200, returns 400 on non-numeric, partial-PATCH semantics, audit-logged. Frontend Tabs: **Calculator** (Pricing+Operational inputs incl. tube-price + tubes-per-session, 4 headline tiles green/rose, Income+Costs breakdown tables, plain-English summary), **Player Benefits** (saving/effective cost/annual saving + break-even table 1×/2×/3× per week), **Old vs New** (3 old-price inputs + Δ£/Δ% comparison table + summary). Currency inputs render £; persists `Math.round(£×100)` pence. Save = explicit PUT, Revert restores last-saved snapshot. Table created via psql `CREATE TABLE IF NOT EXISTS` + `INSERT id=1 ON CONFLICT DO NOTHING`.

### Other modules (full detail in `docs/replit-history.md`)

- **My Training Profile** (`/my-training-profile`, `client/src/pages/MyTrainingProfile.tsx`): Novos.gg-style esports skill dashboard with **admin/coach "View as player" picker** (`isAdminish` → `/api/admin/player-analytics/enrollments?clubId=&type=LEAGUE|PREMIUM`, swaps `effectivePlayerId`), Pick-club selector for admins. Mood photos via `imageForCategory()` keyword mapper (`client/src/assets/skills/*.png`).
- **Dashboard hero** (`client/src/components/dashboard/DashboardHero.tsx`): 13-tile uniform 3-per-row hero — greeting+clock with avatar + time-of-day palette, weather (Open-Meteo), counters, week strip, up-next, training challenges, live courts, today's deal (AI-sourced via OpenAI Responses API web_search → `GET /api/daily-content/deals`), pro tip, hydration, daily mindset quote (`GET /api/daily-content/quote`), daily poll (`GET/POST /api/daily-content/poll[/vote]`), next event. AI content cached daily in `server/dailyContent.ts` with hard fallbacks.
- **Dashboard hero banner + Custom Polls** (`client/src/components/dashboard/DashboardBanner.tsx` + `CustomPollTile.tsx`): cinematic 8-bg cycler (cross-fade 7s). Tables `custom_polls` (audience `ALL|SELECTED`, `targetClubIds int[]`, `target_user_ids int[]`, `send_as_message bool`) + `custom_poll_responses` (unique `(pollId,userId)`, upsert via `onConflictDoUpdate`). Endpoints in `server/customPolls.ts`. SELECTED audience = clubs ∪ users (union). Admin `/admin/polls`. On-demand re-broadcast `POST /api/admin/custom-polls/:id/send-message` writes to `notifications` with `linkUrl /?poll=<id>&t=<ts>` (timestamp = unique row per send). `CustomPollTile` reads `?poll=<id>` and auto-focuses.
- **Premium tile** (`.premium-tile` in `client/src/index.css`): theme-token-aware glassy gradient default for every shadcn `<Card>`. Opt-out: `<Card className="premium-tile-flat">` (used for chart embeds).
- **Coaching hub tabs** (`/coaching`): single sidebar entry, 4 tabs (Find/Lessons/Training Profile/Dashboard, last gated by `isCoachish`). Legacy routes still routable for deep links. `CoachSubNav` returns null on `/coaching`.
- **Sidebar layout** (`client/src/components/layout/Sidebar.tsx`): `collapseToHubs()` collapses activity/club+design/comms+info into hub links. Pinned passthroughs (`pinnedActivityHrefs = ["/sessions"]`) stay visible. Admin sidebar intentionally short — for OWNER: Admin Panel / Financials / Admin Inbox / God Mode only. **Coach Control, Finance Calculator, BSL Control Panel** live as tiles in `/super-admin/god-mode` ("Super Admin Tools" section). **Club Polls** lives as a tile in `/admin` `adminToolsSections`. Push Broadcast / Auto Reminders also surfaced as tiles in `/admin` via `adminToolsSections` + `clubSettingsSections` in `client/src/pages/admin/AdminDashboard.tsx`. `adminInbox` badge piggy-backs on Admin Inbox entry.
- **Team Members on Sessions — multi-assignment**: `playerProfiles.teamRoles text[]` (`COACH`/`SUPPORT_COACH`/`ORGANISER`/`COORDINATOR`/`CUSTOM:<Name>`, label 1-56). Sessions: 4 `int[]` cols `coachUserIds`/`organiserUserIds`/`coordinatorUserIds`/`supportCoachUserIds`. Legacy single-id cols kept for back-compat (mirrored to first array element). `MemberSelector` is multi-select chip picker. Server validates each user is APPROVED member of effective club. `getSessions/getSession` hydrate `*Users` arrays via batched `users` lookup (no N+1). `SessionTeamBadges` renders chip per assignee — Coordinator (gold) > Organiser (blue) > Coach (violet) > Support Coach (emerald).
- **Sessions page views** (`/sessions`): only **Timeline** + **Calendar** modes. localStorage `sessionsViewMode` migration-safe (old `cards`/`grouped` → `timeline`).

## Product

- **Club Management**: Multi-club, memberships, recurring events, sessions, financial intelligence.
- **Player Engagement**: Rankings, match organization, profiles, badges, AI analytics.
- **Admin Tools**: User/club/venue/admin management, audit logs, helpdesk, OWNER-only Control Center.
- **Advanced**: AI reporting, schedule generation, 3D avatars, league, tournaments, merchandise, community hub.
- **Monetization**: Freemium, tiered fees, credit requests, club-scoped referrals.

## User preferences

Preferred communication style: Simple, everyday language.

- **Multi-role users**: `users.secondaryRoles text[] DEFAULT '{}'` — single user can hold multiple roles (e.g. OWNER+COACH). Primary `role` drives badge; `secondaryRoles` is additive. Coach grant flow (`POST /api/admin/users/:id/grant-coach` in `server/coachBookingRoutes.ts`): if PLAYER → primary upgraded to COACH; else COACH appended to `secondaryRoles` (preserves OWNER/ADMIN/ORGANISER). Sidebar "Coach Dashboard" gates on `user.role === "COACH" || secondaryRoles.includes("COACH") || isAdminOrOwner`. Admin role editor (`/super-admin` Users panel in `client/src/pages/admin/ClubManagement.tsx`): primary Select + per-user "Extra roles" chip toggles. `PATCH /api/admin/users/:id/role` (OWNER-only) accepts `{role?, secondaryRoles?}` — server filters secondaryRoles against allowed enum + drops dupes equal to new primary.
- **Training Challenges for All Users** (`/training-challenges`): Reuses exported `ExerciseChallengePanel` from `Juniors.tsx`, passes current user as synthetic juniors array. Read GETs opened (POST/PATCH/DELETE remain premium). **IDOR-safe**: completion + skill-points endpoints enforce self / parent-of-child (via `users.parentUserId`) / OWNER-ADMIN. `isSelfView` prop flips copy.
- **User-editable low-balance alert + Premium membership payment method**: `/profile` Credit Wallet modal → `PATCH /api/my-wallets/:walletId/threshold` writes `wallets.lowBalanceThreshold` (same column as admin UI). Admin "Mark Paid" payment-method dialog: **External bank** (just `paymentConfirmed=true`) OR **Member's wallet credit** (atomic deduction from `credit_ledger` + mirrored DEBIT into `wallets.balance`/`wallet_transactions` inside `db.transaction` with `SELECT … FOR UPDATE` lock — same lockstep as god-mode `set-balance`). Price = `clubMemberships.proratedPrice ?? membershipPlans.annualPrice`. Insufficient credit → 400. Wallet option auto-disables when price=0. `PATCH /api/club-memberships/:id/payment` accepts `paymentMethod: "external" | "wallet"`. Bulk `mark_paid` external-only.
- **Session Financial Snapshot** (`client/src/components/financial/SessionFinancialSnapshot.tsx`): Per-session "View Snapshot" on `/admin/financials`. Mobile-first dialog: header (title/club/venue/date/time/duration/coach), summary tiles, attendees w/ payment-status badges, expenses (informational), **Coach Earnings = full session fee** (NOT collected, NOT minus expenses — coach pays expenses upfront). PNG via html2canvas, multi-page PDF via jsPDF, Web Share API fallback. Backed by `GET /api/sessions/:id` (returns + `coachUser` hydrated from `sessions.coachUserId` w/ creator fallback, gated via `canPerform("VIEW_CLUB", session.clubId)` — never returns creator email).
- **Admin Wallet Unified View + Manual Amend** (`/super-admin/wallet-management`, `WalletManagement.tsx`): "Log" modal mirrors user view (UNION `credit_ledger` + `player_reward_ledger`). Three cards: User sees / Wallet table balance / Drift (amber warning if non-zero). Per-club balance chips, history (200 rows). "Set exact balance" → `POST /api/god-mode/wallets/:walletId/set-balance` `{clubId, targetBalance (pence), reason?}` — computes `delta = target − currentLedgerSum`, atomically writes corrective row to BOTH `credit_ledger` AND `wallets.balance`/`wallet_transactions`. Read: `GET /api/god-mode/wallets/:walletId/unified-view`. OWNER/ADMIN-only with `[AUDIT]` log lines.

## Gotchas

- Express JSON body limit is path-aware in `server/index.ts`: default 256kb, `/api/bsl/clubs*` opts into 8mb for base64 image fallback. Extend `jsonLargeRoutes` to raise per-route — never globally.
- Merchandise Supplier Order Sheet excludes customer contact details (email, phone) for privacy.
- BSL wallet enum `bslWalletTxTypeEnum` is `TOPUP | DEDUCTION` (NEVER `DEBIT`).
- `npm run db:push` may hang on new tables — use `psql "$DATABASE_URL"` with `IF NOT EXISTS` DDL instead.
- All client date keys use a local-time `fmtDate()` (NOT `toISOString().slice(0,10)`) to avoid UTC drift around midnight/DST.

## Pointers

- TanStack React Query: https://tanstack.com/query/latest
- Drizzle ORM: https://orm.drizzle.team/
- Tailwind CSS: https://tailwindcss.com/
- shadcn/ui: https://ui.shadcn.com/
- framer-motion: https://www.framer.com/motion/
- Vite: https://vitejs.dev/
- esbuild: https://esbuild.github.io/
- OpenStreetMap Nominatim: https://nominatim.org/release-docs/latest/api/Search/
- Google Calendar API: https://developers.google.com/calendar/api
